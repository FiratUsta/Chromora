{
    "version": "2.0.3",
    "changelog": [        
    "[SELF HOSTING] Move service worker and manifest to root to avoid scope issues.",
    "The app now uses Noto if the font is installed, or fallsback to other sans-serif fonts instead of trying to import it from Google.",
    "Monochrome palettes now should sort correctly on Chrome."
    ]
}
